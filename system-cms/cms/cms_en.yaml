Error:
  Page:
    Error: Error
    ErrorId: Error id
    ErrorTimestamp: Error Timestamp
    Attributes: Attributes
    ThrownBy: Thrown by
    ProcessCallStack: Process call stack
    TechnicalCause: Technical cause
    RequestUri: Request Uri
    Servlet: Servlet
    Application: Application
    ThreadLocalValues: Request context
    Name: Name
    Value: Value
    StackTrace: Stack Trace
    StartLastProcess:
      Label: Start last process
      Description: Starts the last process that was started with a launch configuration or by pressing x on a start element.
    ContactAdmin: Please contact the system administrator
    Details: Details
    BackToApplication: Back to application
  UnavailablePage:
    Unavailable: ${product} currently not available
    State:
      STARTING: ${product} is starting
      STOPPING: ${product} is stopping
      RUNNING: ${product} is running
      STOPPED: ${product} is stopped
  StatusCode:
    "400": Bad Request
    "401": Unauthorized
    "403": Forbidden
    "404": Not Found
    "409": Conflict
    "500": Server Error
    "503": Service Unavailable
  Exception: 
    ViewExpired: View Expired
    NotFound:
      CaseMap:
        UUID: Case Map with UUID '${uuid}' not found!
        BusinessCase: Business case with id '${businessCaseId}' not found.
        BusinessCaseIdMustBeNumber: Parameter businessCaseId must be a number
    PermissionDenied:
      CaseMap:
        AnonymousUser: An anonymous user is not allowed to start a case map or a side step.
        ExecuteProcess: No permission to execute process '${processId}'
    Request:
      App:
        InMaintenace: The application '${appName}' is currently unavailable because of maintenance.
        NameMissing: Application name missing
        NotFound: The requested application was not found
      CaseMap:
        NoProcessId: Side step process parameter '${processId}' must be defined!
        RequestPathNoUUID: No Case Map UUID found in request path
        WrongFormat: Requested Case Map UUID has wrong format!
      BeanNotFound: Bean '${object}' not found on request. Filter missing?
      ContentObject:
        IsNull: Content object '${object}' has no value
        IsNoPage: Content object '${object}' cannot be used as page.
        NotFound: The content object '${object}' was not found
        NotResolved: Not content object value resolved for '${object}'.
      Dialog:
        IdNotFound: Dialog runtime '${dialogId}' not found
        NotAvailable: Dialog Runtime no longer available
        NotFound: Requested user dialog not found
        RequestMustBeGuid: Requested dialog runtime id must be a GUID
      Engine:
        NotRunning: Process engine should be in running state, but is not!
        NotStarted: The process engine is not running. Please start the process engine first
      File:
        LoadingEx: Exception while loading requested file
        NameMissing: Missing file area name
        NameUnknown: Requested file area name is unknown.
        NotFound: Requested file not found
        PathNotFound: File '${filePath}' not found.
      Licence:
        ClusterLimit: Licence violation cluster node not licenced because cluster nodes limit is exceeded
        NoPublicUrl: Licence violation requested URL does not match any of the licenced public URLs
      Param:
        Missing: Parameter '${parameter}' is missing
        NotValid: Parameter '${parameter}' must be a valid long number
        NotValidChars: Parameter '${parameter}' contains not allowed characters
      PathNoDialogId: No UserDialogId found in request path
      PageContentNotFound: Requested page content object not found.
      PageNoHttpResponse: Auto process page could not be written to HTTP response. 
      PageEngine:
        InvalidRequest: Unsupported request type ${type} 
        WrongState: PageEngine state should be 'RUNNING' but was '${serviceState}'
      Pm:
        InMaintenace: The process model '${pmName}' of application '${appName}' is currently unavailable because of maintenance.
        NotFound: The requested process model of application '${appName}' was not found.
      Pmv:
        InMaintenance: The process model version '${pmvId}' of process model '${pmName}' of application '${appName}' is currently unavailable because of maintenance.
        NameIsMissing: "Process model version name missing"
        NotReleased: There is no process model version released for process model '${pmName}' of application '${appName}'.
        NotFound: The requested process model version was not found.
        PmNotFound: The requested process model version of process model '${pmName}' of application '${appName}' was not found.
      Render:  
        NoTxtObject: Not a text type object '${object}'
        NoImageObject: Not an image '${object}'
        NoMimeObject: Cannot renderer image to mime type '${object}'
        NoResponse: There is no renderer response set
      RestPathMissing: Rest path missing
      SessionExpired: Your session has expired.
      Start:
        NotFoundPmv: The requested process start in process model version '${pmvId}' of process model '${pmName}' of web application '${appName}' was not found.
        NotReleasedPmv: It is not allowed to start a case on the process model version '${pmvId}' of process model '${pmName}' of web application '${appName}' because it is not released.
      Task:
        Forbidden : You are not allowed to work on the task with the identifier '${taskId}'.
        NotFound: The task with the identifier '${taskId}' does not exist.
        InvalidPath: Invalid request path of Task
        IsDestroyed: You can no longer work on the task '${taskName}' with the identifier '${taskId}' because the task was meanwhile destroyed.
        IsDone: You cannot work on the task '${taskName}' with the identifier '${taskId}' because the user '${userName}' has already finished it.
        InProcess: You cannot work on the task '${taskName}' with the identifier '${taskId}' because the user '${userName}' is currently working on it.
        UriOnWrongApp: The application in request uri is not equal with the application of the task.
        MustBePreAnalyzed: Task parameter must be analyzed by earlier filters
      UrlWrongType: Requested URL has an invalid request type '${path}'.
      ViewFileMissing: View file missing
      WrongPathFormat: Wrong Path format
Notification:
  business:
    Description: This notification will be sent if a business relevant change happens in a process.
    DisplayName: Business Information
    Labels:
      Actions:
        Details: Case Details
      Footer:
        Sender: This email was sent by application
        Settings: Manage notifications
      Greeting: Hello
      GreetingShort: Hi
      Title: Business Information
    Templates:
      Default:
        mail:
          Subject: <%= message %>
        web:
          Subject: <%= ivy.html.escape(message) %>
  new-task:
    Description: This notification is sent whenever a new task is assigned to you.
    DisplayName: New task assigned
    Labels:
      Actions:
        Details: Task Details
        Start: Start Task
      Description: The following task has been assigned to you and is waiting to be processed
      Footer:
        Sender: This email was sent by application
        Settings: Manage notifications
      Greeting: Hello
      GreetingShort: Hi
      Task:
        Created: Date created
        Description: Description
        Expiry: Expiry date
        Name: Name
        Priority: Priority
        PriorityLabels:
          EXCEPTION: Exception
          HIGH: High
          LOW: Low
          NORMAL: Normal
        Responsible: Responsible
        Title: Task details
      Title: A new task was assigned to you
    Templates:
      Default:
        mail:
          Subject: 'New Task "<%= ivy.html.escape(ivy.task.name) %>" assigned to: <%= ivy.html.escape(ivy.task.responsibles().displayName()) %>'
        web:
          Subject: New Task '<%= ivy.html.escape(ivy.task.name) %>' for <%= ivy.html.escape(ivy.task.responsibles().displayName()) %>
